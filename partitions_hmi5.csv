# CrowPanel HMI 5.0 (p/n 502727) — 4 MB flash. The ESP32-S3 RGB firmware is ~1.9 MB,
# too big for dual 1.5 MB OTA slots + a usable LittleFS, so this is a SINGLE app slot
# (no OTA — flash over USB via the web flasher) with a generous FS for the runtime caches.
# Revisit (restore dual-OTA) if the firmware ever shrinks under ~1.4 MB.
# Name,     Type, SubType,  Offset,   Size
nvs,        data, nvs,      0x9000,   0x5000
otadata,    data, ota,      0xe000,   0x2000
app0,       app,  ota_0,    0x10000,  0x2C0000
littlefs,   data, spiffs,   0x2D0000, 0x130000
